---
import type { ChipItem } from "@/data/stack";

interface Props {
  items: ChipItem[];
}

const { items } = Astro.props;
---

<ul class="inline-list">
  {
    items.map((item) => (
      <li>
        {item.url ? (
          <a href={item.url}>
            {item.title}
          </a>
        ) : (
          <span>{item.title}</span>
        )}
      </li>
    ))
  }
</ul>
